data:
  img_size: 224
  batch_size: 32
  train_split: 0.8
  val_split: 0.1
  test_split: 0.1
  num_workers: 2
  seed: 42

model:
  learning_rate: 0.001
  epochs: 10
  num_classes: 2
  dropout: 0.5
  architecture: "baseline_cnn"   # options: baseline_cnn | resnet18